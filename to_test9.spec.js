const expect = chai.expect;

 
describe('Exercice 9', () => {
  describe('InnerHTML', () => {
    it('the innerHTML should equal Rocket Cars', () => {
      expect(var1).to.equal('Test');
      expect(var1).to.be.a('string')
    });
  });
  describe('HTML Element Value', () => {
    it('The HTML element value should be car', () => {
      expect(var2).to.equal('car');
      expect(var2).to.be.a('string');
    });
  });
});


