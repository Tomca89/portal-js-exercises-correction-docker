const expect = chai.expect;

 
describe('Exercise JqueryCss', () => {
  describe('The model should change color', () => {
    it('The text should go red', () => {
      expect(color).to.equal('rgb(255, 0, 0)');
    });
  });
});


